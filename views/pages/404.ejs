<!DOCTYPE html>
<% if (language==="ar") { %>
<html lang="ar" dir="rtl"></html>
<% } else { %>
<html lang="en">
  <% } %>
  <head>
    <%- include('../partials/head',{language}); %>
    <link rel="stylesheet" href="/style/404.css" />

    <title>EffectiveSA - <%= tagline %></title>
  </head>
  <body>
    <%- include('../partials/header',{language}); %>
    <div class="container">
      <div class="not-found-card">
        <h1>404</h1>
        <h2>
          <% if (language==="ar") { %> خطأ <% } else { %> Page Not Found <% } %>
        </h2>
        <p>
          <% if (language==="ar") { %> الصفحة التي تبحث عنها غير موجوده <% }
          else { %> The page you are looking for does not exist. <% } %>
        </p>
        <a class="homepage-link" href="/Homepage"
          ><% if (language==="ar") { %> العودة للصفحه الرئيسية<% } else { %>Go
          back to Home<% } %></a
        >
      </div>
    </div>
    <%- include('../partials/footer',{language}); %>
  </body>
</html>
